const selectElement = (element, parentElement = document) => parentElement.querySelector(element);
const elProducts = selectElement('#product_template1')

function renderTemplate(templateArr, element){

}

renderTemplate(products, )